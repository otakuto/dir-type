# Installation

Clone the repository and install directly from the `dir-type-cli` crate.

```console
$ git clone https://github.com/otakuto/dir-type.git
$ cd dir-type
$ cargo install --path dir-type-cli
```

After installation, the `dir-type` command is available.

```console
<!-- cmdrun dir-type --help -->
```
